import { CircularProgress } from "@mui/material";

const Spinner = () => {
  return (
    <div className="w-full centering h-52">
      <CircularProgress />
    </div>
  );
};

export default Spinner;
